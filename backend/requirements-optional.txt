# Optional / Phase-2+ dependencies (install when you need these features)
#
# Postgres driver (only needed if DATABASE_URL is postgres://...)
psycopg2-binary==2.9.9
#
# Blockchain
web3==6.11.3
#
# Image / ML / Audio (can be problematic on newest Python without wheels)
Pillow==10.2.0
opencv-python==4.9.0.80
transformers==4.36.2
librosa==0.10.1
#
# PDF generation + email
reportlab==4.0.7
sendgrid==6.11.0
#
# Utilities
python-dateutil==2.8.2

